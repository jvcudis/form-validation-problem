import React from 'react'
import { shallow } from 'enzyme'
import CustomSelect from '../../components/CustomSelect'

// TODO
describe('<CustomSelect />', () => {
  it('renders without crashing', () => {
    shallow(<CustomSelect />)
  })
})
